#ifndef ARAXCONTROLLER_CUDA_BUILTINS
#define ARAXCONTROLLER_CUDA_BUILTINS

void cuda_alloc_no_throttle(arax_data_s *data);

void cuda_free_no_throttle(arax_data_s *data);

void cuda_free_no_throttle(void *data);

#endif
